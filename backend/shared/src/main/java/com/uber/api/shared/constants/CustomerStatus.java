package com.uber.api.shared.constants;

public enum CustomerStatus {
    AVAILABLE,    // Ready for new rides
    REQUESTING,   // Currently requesting a ride
    ON_RIDE      // Currently on a ride
}
